using System;
using System.Collections.Generic;
using System.Text;

namespace Common
{
    // This file serves absolutely no useful purpose
    // It exists purely because it once existed, and Visual Studio latched on to it so hard it refuses to compile any project without it.
}
