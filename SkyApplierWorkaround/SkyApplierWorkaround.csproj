<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="15.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Import Project="$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props" Condition="Exists('$(MSBuildExtensionsPath)\$(MSBuildToolsVersion)\Microsoft.Common.props')" />
  <Import Project="$(SolutionDir)\GameDir.targets" />
  <Import Project="$(SolutionDir)\Configurations.targets" />
  <Import Project="$(SolutionDir)\References.targets" />
  <PropertyGroup>
    <Configuration Condition=" '$(Configuration)' == '' ">Subnautica</Configuration>
    <Platform Condition=" '$(Platform)' == '' ">AnyCPU</Platform>
    <ProjectGuid>{C287725A-291A-4402-A710-18D79FE84203}</ProjectGuid>
    <OutputType>Library</OutputType>
    <AppDesignerFolder>Properties</AppDesignerFolder>
    <RootNamespace>SkyApplierWorkaround</RootNamespace>
    <AssemblyName>SkyApplierWorkaround</AssemblyName>
    <OutputPath>$(SolutionDir)$(AssemblyName)\$(Configuration)\</OutputPath>
    <TargetFrameworkVersion>v4.7.2</TargetFrameworkVersion>
    <FileAlignment>512</FileAlignment>
    <Deterministic>true</Deterministic>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <Optimize>true</Optimize>
    <PlatformTarget>x64</PlatformTarget>
    <ErrorReport>prompt</ErrorReport>
  </PropertyGroup>
  <Import Project="$(SolutionDir)\References.targets" />
  <ItemGroup>
    <Compile Include="SAWPlugin.cs" />
    <Compile Include="Properties\AssemblyInfo.cs" />
    <Compile Include="SkyApplierPatches.cs" />
  </ItemGroup>
  <Import Project="$(SolutionDir)\Common\Common.projitems" Label="Shared" />
  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
  <Import Project="$(SolutionDir)\PostBuildEvents.targets" />
</Project>